{
  "_comment": [
    "Pumex CL Analytics (TAC 239) — Goldsky cl-analytics-tac/v1.0.1 vs HyperIndex ccf39cd.",
    "hyperindex-schema.graphql is generated via `pnpm introspect` (flat *_id FKs).",
    "Swap is idMatchConfirmed so --deep does not silently skip it (name heuristic).",
    "SwapFeeCache TOO — detectIdMismatch() in src/schema/configGenerator.ts tests",
    "entityName.includes('Swap'), so the substring catches it. Listing only 'Swap'",
    "left SwapFeeCache SKIPPED and reported as 0/0 'All records match!', i.e. a",
    "clean row for an entity that was never compared. Found 2026-08-13; it is the",
    "only unwhitelisted match of that heuristic across all five configs.",
    "Fee-cache / PositionTransferCache entities are ephemeral subgraph helpers —",
    "compare them, but expect possible emptiness differences if Goldsky pruned them."
  ],

  "fieldMappings": {},

  "knownIdMismatch": [],

  "idMatchConfirmed": [
    "Swap",
    "SwapFeeCache"
  ],

  "skipFields": {
    "Token": ["whitelistPools"],
    "Transaction": ["mints", "burns", "swaps", "collects", "flashes"]
  },

  "skipEntities": []
}
